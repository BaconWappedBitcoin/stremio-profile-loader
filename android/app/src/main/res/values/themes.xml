<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform Material theme, no action bar. The picker draws its own dark UI. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/loader_bg</item>
        <item name="android:statusBarColor">@color/loader_bg</item>
        <item name="android:navigationBarColor">@color/loader_bg</item>
    </style>
</resources>
